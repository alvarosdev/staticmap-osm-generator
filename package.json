{
  "name": "test",
  "module": "src/server/index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development bun --watch run src/server/index.ts",
    "start": "NODE_ENV=production bun run src/server/index.ts",
    "clean:cache": "rm -rf cache"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.2",
    "sharp": "^0.34.4"
  }
}
